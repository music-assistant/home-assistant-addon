configuration:
  name:
    name: Player name
    description: >-
      The name this player shows up under in Music Assistant. Leave it empty and
      it uses this system's hostname, which is usually "homeassistant".
  output:
    name: Audio output
    description: >-
      Where to play. Leave this at `default` to use Home Assistant's audio
      system, and pick the sound card in this app's Audio panel instead. "null"
      (quoted, when editing as YAML) throws the audio away, which is handy for
      checking that the player shows up in Music Assistant before any speakers
      are wired in. Sound-card names like `hw:1,0` cannot be opened from this
      app and stop it — the Audio panel is how a card is chosen.
  log_level:
    name: Log level
    description: >-
      How much this player writes to its log. Raise it to debug while chasing a
      problem, and put it back afterwards.
  server:
    name: Music Assistant server
    description: >-
      Leave this empty and the player waits to be found over mDNS, which is what
      you want. Set it to a host, host:port or ws:// URL only if the server
      cannot discover the player by itself — doing so also stops the player
      being discoverable. Do not put a password in here; it is stored and shown
      as plain text.
